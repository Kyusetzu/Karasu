import { Card, CardTitle } from "karasu";

export const Default = () => (
  <div style={{ padding: 16 }}>
    <Card>
      <CardTitle>Continue watching</CardTitle>
      <p className="mt-1 text-sm text-ink-300">A title, then the body underneath it in the quieter ink.</p>
    </Card>
  </div>
);
