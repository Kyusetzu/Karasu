<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Hand-added: setPersisted(true) on the notification job throws without
         it. The notification plugin's AAR happens to donate the same
         permission through the manifest merge, but a persisted job must not
         depend on a dependency's manifest — the merger dedupes. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- AndroidTV support -->
    <uses-feature android:name="android.software.leanback" android:required="false" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.karasu"
        android:usesCleartextTraffic="${usesCleartextTraffic}">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:launchMode="singleTask"
            android:label="@string/main_activity_title"
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <!-- AndroidTV support -->
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
            <!-- DEEP LINK PLUGIN. AUTO-GENERATED. DO NOT REMOVE. -->
            <intent-filter android:autoVerify="true" >
                <action android:name="android.intent.action.VIEW" />
                <!-- ChromeOS ARC++ uses a different action for deep links -->
                <action android:name="org.chromium.arc.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" />
                <data android:scheme="http" />
                <data android:host="anilist.co" />
                
                
                <data android:pathPrefix="/anime" />
                <data android:pathPrefix="/manga" />
                <data android:pathPrefix="/user" />
                <data android:pathPrefix="/forum/thread" />
                <data android:pathPrefix="/character" />
                <data android:pathPrefix="/staff" />
                <data android:pathPrefix="/studio" />
                
            </intent-filter>
            <intent-filter android:autoVerify="true" >
                <action android:name="android.intent.action.VIEW" />
                <!-- ChromeOS ARC++ uses a different action for deep links -->
                <action android:name="org.chromium.arc.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" />
                <data android:scheme="http" />
                <data android:host="www.anilist.co" />
                
                
                <data android:pathPrefix="/anime" />
                <data android:pathPrefix="/manga" />
                <data android:pathPrefix="/user" />
                <data android:pathPrefix="/forum/thread" />
                <data android:pathPrefix="/character" />
                <data android:pathPrefix="/staff" />
                <data android:pathPrefix="/studio" />
                
            </intent-filter>
            <intent-filter >
                <action android:name="android.intent.action.VIEW" />
                <!-- ChromeOS ARC++ uses a different action for deep links -->
                <action android:name="org.chromium.arc.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="karasu" />
                
                
                
                
                
            </intent-filter>
            <!-- DEEP LINK PLUGIN. AUTO-GENERATED. DO NOT REMOVE. -->
            <!-- Hand-added, OUTSIDE the markers above (that block is
                 rewritten on every build): the share target. MainActivity
                 rewrites a SEND into a synthetic VIEW so the deep-link
                 plugin's own validation and routing carry it — see the
                 comment there. -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

        <provider
          android:name="androidx.core.content.FileProvider"
          android:authorities="${applicationId}.fileprovider"
          android:exported="false"
          android:grantUriPermissions="true">
          <meta-data
            android:name="android.support.FILE_PROVIDER_PATHS"
            android:resource="@xml/file_paths" />
        </provider>

        <!-- Hand-added, outside the deep-link markers (that block is
             rewritten on every build): the background notification job.
             See NotifJob.kt for the whole story. -->
        <service
          android:name=".NotifJobService"
          android:permission="android.permission.BIND_JOB_SERVICE"
          android:exported="false" />
    </application>
</manifest>
